```mermaid
```mermaid
erDiagram

    USER "用户" ||--o{ USER_ROLE "用户角色" : "绑定角色"
    ROLE "角色" ||--o{ USER_ROLE "用户角色" : "包含用户"
    ROLE "角色" ||--o{ ROLE_PERMISSION "角色权限" : "分配权限"
    PERMISSION "权限菜单" ||--o{ ROLE_PERMISSION "角色权限" : "属于角色"

    USER "用户" }o--|| DEPARTMENT "部门" : "所属部门"
    DEPARTMENT "部门" ||--o{ DEPARTMENT_HEADER "部门负责人" : "设置负责人"
    USER "用户" ||--o{ DEPARTMENT_HEADER "部门负责人" : "担任负责人"

    CURRICULUM "训练计划" ||--o{ COURSE_RESOURCES "课程资源" : "包含动作"
    CURRICULUM "训练计划" ||--o{ TIMETABLE "排课记录" : "排课"
    CURRICULUM "训练计划" ||--o{ ASSIGNMENT "训练打卡" : "打卡任务"

    CURRICULUM "训练计划" }o--|| USER "用户" : "教练"

    TIMETABLE "排课记录" }o--|| USER "用户" : "授课教练"
    TIMETABLE "排课记录" }o--|| USER "用户" : "参课会员"

    ASSIGNMENT "训练打卡" }o--|| USER "用户" : "打卡学员"

    APPRAISE "身体追踪" }o--|| USER "用户" : "被测会员"
    BODY_PROFILE "身体档案" }o--|| USER "用户" : "建档会员"

    NUTRITION "饮食补剂" }o--|| USER "用户" : "制定教练"
    NUTRITION "饮食补剂" }o--|| USER "用户" : "服务会员"

    MESSAGE "交流互动" }o--|| USER "用户" : "发布者"
    MESSAGE "交流互动" }o--|| USER "用户" : "回复者"

    DICT "字典类型" ||--o{ DICT_DATA "字典数据" : "字典项"

    USER "用户" {
        string id PK "用户唯一标识"
        string username "登录用户名"
        string password "密码BCrypt加密"
        string nickname "昵称"
        string mobile "手机号"
        string email "邮箱"
        int type "0=会员 1=管理员 2=教练"
        string department_id FK "所属部门ID"
        string level "教练级别"
        string specialties "擅长方向"
    }

    ROLE "角色" {
        string id PK "角色唯一标识"
        string name "角色名称"
        bit default_role "是否默认角色"
        int data_type "数据权限类型"
    }

    PERMISSION "权限菜单" {
        string id PK "权限唯一标识"
        string name "权限名称"
        string title "菜单标题"
        string parent_id FK "父级ID"
        int type "0=导航 1=页面 2=按钮"
    }

    USER_ROLE "用户角色" {
        string id PK "关联ID"
        string user_id FK "用户ID"
        string role_id FK "角色ID"
    }

    ROLE_PERMISSION "角色权限" {
        string id PK "关联ID"
        string role_id FK "角色ID"
        string permission_id FK "权限ID"
    }

    DEPARTMENT "部门" {
        string id PK "部门唯一标识"
        string title "部门名称"
        string parent_id FK "父级部门ID"
        bit is_parent "是否父节点"
    }

    DEPARTMENT_HEADER "部门负责人" {
        string id PK "记录ID"
        string department_id FK "部门ID"
        string user_id FK "用户ID"
        int type "负责人类型"
    }

    CURRICULUM "训练计划" {
        string id PK "计划唯一标识"
        string title "计划标题"
        string plan_type "计划类型"
        string difficulty "难度等级"
        string target_muscle "目标肌群"
        decimal price "课程价格"
        string coach_id FK "教练ID"
    }

    COURSE_RESOURCES "课程资源" {
        string id PK "资源唯一标识"
        string curriculum_id FK "所属训练计划ID"
        string action_name "动作名称"
        string action_type "动作类型"
        string target_muscle "目标肌群"
        int sort_order "排序号"
    }

    TIMETABLE "排课记录" {
        string id PK "排课唯一标识"
        string curriculum_id FK "训练计划ID"
        string coach_id FK "教练ID"
        string user_id FK "会员ID"
        string date "排课日期"
        string status "状态"
        string location "上课地点"
    }

    ASSIGNMENT "训练打卡" {
        string id PK "打卡唯一标识"
        string curriculum_id FK "训练计划ID"
        string user_id FK "学员ID"
        string title "打卡标题"
        string file1 "教练资源URL"
        string file2 "学员提交URL"
        int sets "训练组数"
        int reps "每组次数"
    }

    APPRAISE "身体追踪" {
        string id PK "记录唯一标识"
        string member_id FK "会员ID"
        double weight "体重kg"
        double body_fat "体脂率"
        double muscle_mass "肌肉量kg"
        double bmi "身体质量指数"
        string goal_progress "目标进度"
    }

    BODY_PROFILE "身体档案" {
        string id PK "档案唯一标识"
        string member_id FK "会员ID"
        string fitness_goal "健身目标"
        string blood_type "血型"
        string allergies "过敏史"
        double flexibility_score "柔韧性评分"
        double strength_score "力量评分"
        double cardio_score "心肺评分"
    }

    NUTRITION "饮食补剂" {
        string id PK "方案唯一标识"
        string coach_id FK "教练ID"
        string member_id FK "会员ID"
        string goal_type "目标类型"
        int daily_calories "每日卡路里"
        double protein "蛋白质占比"
        double carbs "碳水占比"
        double fat "脂肪占比"
    }

    MESSAGE "交流互动" {
        string id PK "消息唯一标识"
        string user_id FK "发布者ID"
        string content "消息内容"
        string type "消息类型"
        string severity "严重程度"
        int likes "点赞数"
        bit resolved "是否已解决"
        string reply_id FK "回复者ID"
    }

    DICT "字典类型" {
        string id PK "字典唯一标识"
        string title "字典名称"
        string type "字典类型编码"
    }

    DICT_DATA "字典数据" {
        string id PK "数据项ID"
        string dict_id FK "字典类型ID"
        string title "字典标签"
        string value "字典值"
    }

    FILE "文件" {
        string id PK "文件唯一标识"
        string name "文件名"
        bigint size "文件大小"
        string type "文件类型"
        string url "访问URL"
    }

    LOG "操作日志" {
        string id PK "日志唯一标识"
        string name "操作名称"
        int log_type "日志类型"
        string username "操作人"
        string ip "请求IP"
        string request_url "请求URL"
    }
```
```